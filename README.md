# Remote control of coding-agent harnesses

How to discover, attach to, and operate many coding-agent sessions across a laptop, remote hosts and Kubernetes, with chat as one possible operator interface. These notes distinguish documented product capabilities from a proposed architecture; they are not an implementation or a benchmark.

## Questions and answers

1. **How can I centrally manage interactive and unattended agents running locally, on servers, and on Kubernetes, potentially from Slack or Zulip?** Use a small always-on coordinator as the system of record for runs, permissions, commands and artifacts, with host/Kubernetes adapters and chat adapters at its edges. Keep interactive terminal attachment separate from sending structured agent turns. Slack/Zulip should show status and request actions, not be the sole session store. Start with [the control-plane design](control-plane.md) and [chat transport and safety](chat-interface.md). This is an architectural recommendation, not a claim that a single reviewed tool supplies the whole mixed-environment solution.
2. **My actual remote use is on a phone: I keep interactive OMP sessions in tmux at my desk, but want to watch and reply to the same agents in Zulip. What can I borrow from Block's Buzz?** Keep the interactive OMP sessions as the owners; first load a bridge extension into each process (restart/resume uninstrumented hosts under the shared configuration). Mirror meaningful lifecycle updates into one Zulip topic per session and route human replies into the live session through that extension and a local bridge; use explicit delivery receipts and distinguish ordinary follow-ups from interrupts and approvals. A local bridge suffices while one computer is awake; add a hosted coordinator only when other hosts/offline routing demand it. See [the phone-first design](mobile-zulip.md) and [what Buzz actually does](buzz-pattern.md).
3. **Can I start OMP sessions by hand, attach a nearby service over ACP to read their state and map each to a Zulip topic, then create agents for new topics?** The topic-to-session mapping is sound, but `omp acp` is a separate stdio server, not a way to attach to an already-running TUI; loading its saved session into another process is not live attachment. Register each manually started TUI with an in-process OMP extension (or build a Collab guest adapter), then explicitly launch and register another interactive OMP in tmux for an authorized new topic. The earlier chat-only ACP alternative would create the two kinds of agents rejected below. See [the ACP attachment decision](acp-attachment.md).
4. **How do I launch OMP directly in ACP mode and automate new agents, while sometimes joining the same live session through OMP's sharing feature?** An ACP client can spawn `omp acp` over stdio, but that process does not host Collab; `omp join` joins an interactive OMP host. The initial answer offered separate chat-first and interactive modes, which the next question rejects. See [the ACP/Collab constraint and uniform alternative](acp-collab-modes.md).
5. **I do not want two kinds of agents. How does one uniform setup let Zulip address agents that I can also operate interactively?** Every agent is an interactive OMP host with the same globally loaded bridge extension and Collab auto-hosting, whether a human starts it or a supervisor launches it from a Zulip topic. At session start the extension registers the live session ID with a local bridge; the bridge binds it to one Zulip topic. A reply in that topic goes over local IPC into that same process through `pi.sendUserMessage`, while OMP events flow back to the topic. `omp join` is another client of that host; there is no ACP subprocess or duplicate session. See [the phone reply path](mobile-zulip.md) and [the unified host decision](acp-collab-modes.md).
6. **Can we use one harness-agnostic ACP session host so an ordinary ACP client and Zulip, Telegram, or Slack share the same live conversation, including messages typed directly in the client?** Yes as an architecture: the host is the sole ACP client of the agent and fans out accepted user prompts and agent updates to every attached interface. [Hydra ACP](https://github.com/smagnuso/hydra-acp) is a close experimental implementation with an ACP editor shim and a [Slack thread bridge](https://github.com/smagnuso/hydra-acp-slack); Zulip and Telegram adapters are not included in the reviewed Hydra documentation. Standard ACP still does not specify multi-client live attachment. See [the shared ACP session survey](acp-shared-session-products.md) for product boundaries and the direct-client-to-chat visibility requirement.
7. **Would my nearby OML project help manage roughly fifty tmux agents, continue them from a phone, and share only some with teammates—or is a simpler product enough?** As shipped, OML is a local ACP client, not yet a multi-client ACP server or chat frontend. The earlier comparison to tmux, Claude Remote Control, and Hydra missed the proposed **one-OML-instance-per-OMP-agent** topology; question 9 corrects that reading. See [the corrected OML design and alternatives](oml-shared-sessions.md).
8. **Are there ready ACP-in/ACP-out proxies that route or orchestrate agents, so existing Zulip, Slack, or Telegram ACP chat clients can simply point at one?** Yes, for distinct jobs: ACP proxy chains transform a one-client/one-agent path; Hydra owns and multiplexes live ACP sessions, and its separate planner extension coordinates task-DAG workers. None automatically turns a generic chat bridge into an observer of an existing session: such bridges tend to create their own per-topic sessions, miss peer-originated prompts when idle, and have incompatible permission/reconnect paths. Hydra's own Slack bridge is the reviewed ready multi-session integration; a pinned generic-client/shim pair warrants a single-session experiment, not a plug-and-play fleet claim. See [the proxy and chat-client composition test](acp-proxy-composition.md).
9. **Correction: I want one OML in front of each OMP agent, speaking ACP to `omp acp` and accepting ACP clients from desktop, Zulip, and Telegram—not a universal fleet coordinator. Does that work?** Yes as a target architecture: each OML instance owns one OMP ACP process and presents one multi-client ACP-agent facade. A chat *bot bridge*, not Zulip or Telegram itself, attaches via an OML stdio shim; one bot/topic pinned to one instance needs no global router. OML does not yet implement the server facade, idle fan-out, prompt/permission arbitration, replay, or team admission. A native OMP TUI is not an ACP client of its ACP-mode sibling, so desktop control needs an actual ACP frontend or the alternative OMP extension/Collab path. See [the corrected per-agent design](oml-shared-sessions.md) and [chat-client compatibility](acp-proxy-composition.md).
10. **Why not use Microsoft's Agent Host Protocol for the per-agent OML host instead?** We should: [AHP](https://microsoft.github.io/agent-host-protocol/guide/ahp-and-acp) directly defines the missing shared-session coordination over an ACP agent—authoritative state, sequenced turns, permission arbitration, and reconnect replay. Third-party [`ahpd`](https://github.com/softov/ahpd/blob/048b9d6dfda1f7d8808a3a0094b6c6d7c75ef8ef/packages/agent-acp/README.md#L20-L35) can launch a configured ACP backend (`omp acp` is a candidate, not a tested integration), and [`ahpc`](https://github.com/softov/ahpc/blob/4b1de8d219e8b2455c9ea6df3011cd852cd6852b/README.md#L58-L113) supplies a ready terminal AHP client. But existing ACP desktop/Zulip/Telegram clients cannot point at `ahpd --stdio`—that still speaks AHP; they need an ACP-to-AHP facade or conversion to AHP clients, and `ahpd` roles are not per-session ACLs. The catalog's [`pi-ahp`](https://github.com/Qusic/pi-ahp/blob/4065e98309b03c1d2d916300856c3c21a9c10674/src/pi/in-process-backend.ts#L1-L21) embeds a particular pi SDK rather than launching `omp acp`. See [the AHP alternative and explicit tradeoff](oml-shared-sessions.md).
11. **Please verify again: can an ACP client attach to an OMP session that is already running?** Not to an independently started native OMP TUI with stock OMP: it exposes no ACP endpoint for that live process. `omp acp` starts a separate stdio server; loading the TUI's saved session restores history elsewhere rather than attaching. An existing ACP-mode session is addressable through its already-owned ACP connection, not via a fresh stock connection. Native Collab can join a live TUI, while an ACP facade for it would require a separately implemented bridge. See [the rechecked ACP attachment boundary](acp-attachment.md).

## Where to start

- [A control plane for mixed-location harnesses](control-plane.md): runtime/transport choices, session ownership, lifecycle and security.
- [Chat should be an operator interface, not the agent runtime](chat-interface.md): Slack versus Zulip, event recovery, permissions and human-facing commands.
- [The same OMP session on desktop and in Zulip](mobile-zulip.md): phone-first updates, replies and the local bridge.
- [The portable pattern in Block Buzz](buzz-pattern.md): what to copy, and what not to assume of its agent harness.
- [Why stock ACP does not attach to a live OMP TUI](acp-attachment.md): verified process boundary, persisted-session distinction, and native Collab alternative.
- [One interactive OMP host for chat and native guests](acp-collab-modes.md): a uniform execution model regardless of how the agent starts.
- [A shared ACP session host and available bridges](acp-shared-session-products.md): direct clients, group-chat visibility, and existing product gaps.
- [One OML proxy per ACP-speaking OMP agent](oml-shared-sessions.md): per-agent interface, desktop client, chat bridge, and selective sharing.
- [ACP proxy and chat-client composition](acp-proxy-composition.md): proxy-chain versus session-owner semantics and why substituting `agent_cmd` is insufficient.

**Current recommendation:** Keep the user's proposed **one owner per OMP ACP process** seam, but evaluate [AHP/`ahpd`](oml-shared-sessions.md) as the implementation of shared-session coordination before writing it in OML. With AHP-speaking desktop and chat clients, `ahpd` plus its ACP backend can own `omp acp`; with existing ACP desktop and Zulip/Telegram clients, an ACP-agent facade/adapter is still required. Compare that adapter with a native OML ACP-in/ACP-out owner rather than assuming either is free. Put the local client in tmux for hotkey navigation, attach team chat only to selected owners, and keep the native OMP TUI/Collab [mode distinction](acp-collab-modes.md) explicit. The [chat-client compatibility contract](acp-proxy-composition.md) still applies.

**Existing integrated option, with a different scope:** kagent's `AgentHarness` is a Kubernetes resource for OpenClaw or Hermes on Agent Substrate, with kagent UI/ACP chat and documented Slack settings. Its documentation does **not** describe an OMP backend or a mixed laptop/server/Kubernetes inventory. Consider it if those documented runtimes and its substrate match your constraints; do not mistake it for generic attachment to arbitrary already-running harnesses ([concept](https://kagent.dev/docs/kagent/0.x/concepts/agent-harness/), [example](https://kagent.dev/docs/kagent/0.x/examples/agent-harness/)).
